{
  "name": "@oi/extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm exec vite build --watch",
    "build": "pnpm exec vite build && node scripts/post-build.mjs",
    "typecheck": "pnpm exec tsc --noEmit",
    "lint": "pnpm exec eslint src --ext .ts,.tsx"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "@types/chrome": "^0.0.270"
  }
}
